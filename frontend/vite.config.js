import { defineConfig } from 'vite';

export default defineConfig({
  // ফ্রন্টএন্ড ফাইলগুলো কোথায় আছে তা নির্দিষ্ট করে দেওয়া হলো
  root: 'frontend',
  
  // লোকাল সার্ভার কনফিগারেশন (একই ওয়াইফাইয়ের অন্য ডিভাইস থেকে টেস্ট করার জন্য)
  server: {
    host: true, // 0.0.0.0 এ লিসেন করবে, ফলে লোকাল আইপি দিয়ে ফোনেও এক্সেস করা যাবে
    port: 5173,
    strictPort: true,
  },
  
  // প্রোডাকশনে যাওয়ার আগে প্রজেক্ট বিল্ড করার কনফিগারেশন
  build: {
    outDir: '../dist', // বিল্ড করা ফাইলগুলো dist ফোল্ডারে সেভ হবে
    emptyOutDir: true, // নতুন বিল্ডের আগে পুরোনো dist ফোল্ডার ক্লিয়ার করবে
    target: 'esnext',  // মডার্ন ব্রাউজারের জন্য ফাস্ট পারফরম্যান্স নিশ্চিত করবে
  }
});